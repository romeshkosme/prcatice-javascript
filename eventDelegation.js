document.getElementById("category").addEventListener("click", (e) => {
    console.log(e.target)
})

// instead of assigning event listener to multiple html elements we assisgn a single event listener to the parent